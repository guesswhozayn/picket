FROM node:20-alpine

WORKDIR /app

# Install dependencies needed for package builds (optional, e.g. python3, make, g++)
RUN apk add --no-cache python3 make g++

COPY package*.json ./

RUN npm ci --only=production

COPY . .

EXPOSE 5000

ENV NODE_ENV=production

CMD ["node", "server.js"]
